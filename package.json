{
  "scripts": {
    "build": "mkdir dist && cp index.html index.tsx App.tsx types.ts metadata.json dist/ && cp -r components hooks services dist/"
  },
  "dependencies": {
    "@google/genai": "^1.22.0"
  }
}
